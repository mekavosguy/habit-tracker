services:
  - type: web
    name: habit-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: habit-db
          property: connectionString

databases:
  - name: habit-db
    plan: free
